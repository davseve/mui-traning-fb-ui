import { createTheme } from "@mui/material";

export const theme = createTheme( {
    palette: {
        primary: {
            main: '#ffff00',
            light: '#ff8a80',
        },
        secondary: {
            main: '#2979ff',
        },
        otherColor: {
            main: '#f50057',
        }
    },
} )